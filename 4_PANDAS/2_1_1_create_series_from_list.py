# we can able to create series with...
# list tuple dictionary and numpy array
# we can't create series with set because...
# set is unoreder datatype

# create series with python list
import pandas as pd
A = [1,2,3,4]
B = pd.Series(A)
print(B)